/**
 * Firebase Stub
 * Currently using HTTP polling only - Firebase will be configured later
 */

// Stub objects to prevent import errors
const app = null;
const db = null;

console.log('ℹ️ Using HTTP polling for slot updates (Firebase not configured yet)');

export { app, db };
